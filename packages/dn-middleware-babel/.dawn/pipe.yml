init:
  - name: pkginfo

dev:
  - name: $local
    location: ./lib/index.js

test:
  - name: lint

build:
  - name: shell
    script:
      - babel src -d lib --extensions ".ts" --copy-files

publish:
  - name: call
    pipe:
      - test
  - name: publish
