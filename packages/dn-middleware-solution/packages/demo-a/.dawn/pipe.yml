init:
  - name: pkginfo

dev:
  - name: shell
    script:
      - node ./lib/index.js

build:
  - name: shell
    script:
      # - dn dev
      - echo '#${process.env.DN_ENV}#'

test:
  - name: lint
  - name: unit

publish:
  - name: publish 