const XXX = 0;

/**
 * 这是测试
 * @returns {void} 无返回
 */
function test() {
  switch (XXX) {
    case 'a':
      break;
    case 'b':
      break;
  }
  return <XXX />;
}
test();
console.log(1);

// const foo = 1;
// const bar = 2;
